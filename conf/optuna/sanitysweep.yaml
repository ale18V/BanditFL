# Optuna study direction for the scalar objective returned by the sweep (validation accuracy).
direction: maximize

search_space:
  heterogeneity.alpha:
    name: alpha
    type: categorical
    choices: [0.1, 0.5]
  topology.neighbor_sampler:
    name: sampler
    type: categorical
    choices: [uniform, bandit]
  topology.sampling:
    type: categorical
    choices: [0.01, 0.1]
    when:
      topology.neighbor_sampler: [uniform, bandit]
