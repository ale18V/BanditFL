defaults:
  - profile: cifar_dynamic
  - train: dynamic
  - _self_

seed: 0
device: auto

hydra:
  run:
    dir: .hydra_runs/${now:%Y-%m-%d}/${now:%H-%M-%S}
  sweep:
    dir: .hydra_multirun/${now:%Y-%m-%d}/${now:%H-%M-%S}
