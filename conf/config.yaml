defaults:
  - profile: mnist
  - topology: dynamic_uniform
  - adversary: none
  - _self_
  - override

seed: 123
device: "cpu"

hydra:
  run:
    dir: .hydra_runs/${now:%Y-%m-%d}/${now:%H-%M-%S}
  sweep:
    dir: .hydra_multirun/${now:%Y-%m-%d}/${now:%H-%M-%S}
